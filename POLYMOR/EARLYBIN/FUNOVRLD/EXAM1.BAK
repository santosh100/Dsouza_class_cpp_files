			// THIS PROGRAM SHOWS FUNCTION OVERLOADING ( DON'T FORGET BEGGARS ARE NOT CHOOSERS)
#include<iostream.h>
#include<conio.h>
int add(int a,int b)
{
	return(a+b);
}
int add(int a,int b,int c)
{
	return(a+b+c);
}
int add(float a,float b)
{
	return(a+b);
}
void main()
{
	clrscr();
    //	cout<<"\n"<<add(10,20,30);
   //	cout<<"\n"<<add(10,20);
   //	cout<<"\n"<<add(2.5f,3.5f);
	cout<<"\n"<<add(2.5,3.5);         // It is like son which has choice (becouse tere are to add functions with two parameters) to choose money. hence ambiguity error
    //	cout<<"\n"<<add(10.5,20.5,30.5);  // It is like beggar it dosen't have any choice hence no error
	getch();
}